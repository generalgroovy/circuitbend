{
  "name": "circuitbend-sandbox",
  "version": "0.7.0",
  "private": true,
  "description": "Local media sandbox with compact tools, portable sample projects, reversible effects and saved experiments",
  "scripts": {
    "test": "node tests/smoke.mjs && node tests/pages.mjs",
    "test:browser": "node tests/workspace.browser.mjs && node tests/lifecycle.browser.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
