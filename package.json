{
  "name": "circuitbend-sandbox",
  "version": "0.8.0",
  "private": true,
  "description": "A local signal workbench for images, mathematical motion and reversible visual experiments",
  "scripts": {
    "test": "node tests/smoke.mjs && node tests/pages.mjs && node tests/docs.mjs",
    "test:browser": "node tests/workspace.browser.mjs && node tests/lifecycle.browser.mjs && node tests/design.browser.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
